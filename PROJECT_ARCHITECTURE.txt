# 📊 PROJECT ARCHITECTURE & VISUALIZATION

## Complete Project Structure

```
Verifiable-Stubs/
├── 📄 ROOT CONFIGURATION FILES
│   ├── run.py                       # ✅ Application entry point
│   ├── config.py                    # ✅ Config (Dev/Prod/Test)
│   ├── requirements.txt             # ✅ Python dependencies
│   ├── .env                         # ✅ Environment variables
│   ├── .gitignore                   # ✅ Git ignore rules
│   ├── Dockerfile                   # ✅ Docker image config
│   └── docker-compose.yml           # ✅ PostgreSQL stack
│
├── 📁 APP/ (Main Application)
│   ├── __init__.py                  # ✅ Flask app factory
│   │
│   ├── 📁 models/ (Database Layer)
│   │   ├── __init__.py
│   │   ├── doctor.py               # ✅ 7 models (Doctor, Degree, etc.)
│   │   ├── academic.py             # ✅ 4 models (Student, Record, Rules)
│   │   └── insurance.py            # ✅ 4 models (Medical, Review, etc.)
│   │
│   ├── 📁 routes/ (API Endpoints)
│   │   ├── __init__.py
│   │   ├── doctor_onboarding.py    # ✅ 9 doctor endpoints
│   │   ├── academic_admission.py   # ✅ 7 academic endpoints
│   │   └── insurance_claim.py      # ✅ 8 insurance endpoints
│   │
│   ├── 📁 schemas/ (Request Validation)
│   │   ├── __init__.py
│   │   ├── doctor_schema.py        # ✅ Doctor schemas
│   │   ├── academic_schema.py      # ✅ Academic schemas
│   │   └── insurance_schema.py     # ✅ Insurance schemas
│   │
│   └── 📁 utils/ (Utilities)
│       └── __init__.py             # ✅ Helper functions
│
├── 📁 migrations/ (Database Schema Versioning)
│   ├── __init__.py
│   ├── env.py                      # ✅ Alembic configuration
│   └── 📁 versions/
│       ├── __init__.py
│       └── 001_initial.py          # ✅ Initial schema (18 tables)
│
├── 📚 DOCUMENTATION
│   ├── README.md                    # ✅ Project overview
│   ├── SETUP_INSTRUCTIONS.txt       # ✅ Setup guide (3 steps)
│   ├── QUICK_REFERENCE.txt          # ✅ Command reference
│   ├── AWS_DEPLOYMENT.md            # ✅ Deployment guide
│   ├── PROJECT_SUMMARY.txt          # ✅ Complete summary
│   ├── API_EXAMPLES.rest            # ✅ API request examples
│   └── openapi_schema.json          # ✅ OpenAPI specification
│
└── 🔧 SETUP SCRIPTS
    ├── setup.bat                    # ✅ Windows setup
    └── setup.sh                     # ✅ Linux/Mac setup
```

---

## Request Flow Diagram

```
┌──────────────────────────────────────────────────────────────┐
│                    HTTP REQUEST                               │
│         (POST/GET to /api/v1/*)                               │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│               FLASK BLUEPRINT ROUTING                         │
│      (doctor_bp, academic_bp, insurance_bp)                   │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│              ROUTE HANDLER FUNCTION                           │
│    (doctor_onboarding.py / academic_admission.py / etc)       │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│            PYDANTIC SCHEMA VALIDATION                         │
│    (doctor_schema / academic_schema / insurance_schema)       │
└────────────────────────┬─────────────────────────────────────┘
                         │
                    ┌────┴────┐
                    │          │
                    ▼          ▼
          ┌──────────────┐  ┌──────────────┐
          │ VALID DATA   │  │ INVALID DATA │
          └────────┬─────┘  └────────┬─────┘
                   │                 │
                   ▼                 ▼
        ┌──────────────────┐  ┌─────────────────┐
        │ Database Query   │  │ Return 400 Error│
        │ (SQLAlchemy)     │  └─────────────────┘
        └────────┬─────────┘
                 │
     ┌───────────┼───────────┐
     │           │           │
     ▼           ▼           ▼
  ┌──────┐  ┌──────┐  ┌──────────┐
  │INSERT│  │SELECT│  │UPDATE/DEL│
  └───┬──┘  └───┬──┘  └────┬─────┘
      │        │           │
      └────────┼───────────┘
               │
               ▼
┌──────────────────────────────────────────────────────────────┐
│              POSTGRESQL DATABASE                              │
│ (18 tables with proper relationships & indexes)              │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│             CONSTRUCT JSON RESPONSE                           │
│              (doctor.to_dict() / etc)                         │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│               RETURN HTTP RESPONSE                            │
│         (200/404/500 + JSON data)                             │
└──────────────────────────────────────────────────────────────┘
```

---

## Database Relationship Diagram

### DOCTOR SCHEMA
```
doctors (8 cols)
├── degrees (1:many)
├── board_certifications (1:many)
├── trainings (1:many)
├── employments (1:many)
├── disciplinary_actions (1:many)
└── malpractice_cases (1:many)

Key Fields:
- doctor_id (unique, indexed)
- license_number (unique, indexed)
- Foreign keys link to all related tables
```

### ACADEMIC SCHEMA
```
student_seeds (4 cols)
├── academic_records (1:many)

eligibility_rules
├── program (unique, indexed)
└── category_specific (JSON field)

merit_rules
├── program (unique, indexed)
└── weightage (JSON field)

Key Fields:
- student_id (unique, indexed)
- roll_number (indexed)
- certificate_number (unique, indexed)
```

### INSURANCE SCHEMA
```
medical_enrichment_requests
├── diagnosis
├── hospital_name
└── icd_mapping (JSON)

reviewer_requests
└── workflow_state (JSON)

payout_requests
├── approved_amount
├── deductible
└── policy_limit

notification_requests
├── recipient_email
├── subject
├── message
└── sent (boolean)
```

---

## API Endpoint Map

```
┌─────────────────────────────────────────────────────────────┐
│           VERIFIABLE STUBS API (26 ENDPOINTS)               │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  HEALTH CHECK (1)                                           │
│  └─ GET /api/v1/health                                      │
│                                                              │
│  DOCTOR ONBOARDING (9)                                      │
│  ├─ POST /api/v1/doctors/admin/add                          │
│  ├─ GET /api/v1/doctors/psv/license/status                  │
│  ├─ GET /api/v1/doctors/psv/degree/info                     │
│  ├─ GET /api/v1/doctors/psv/board-cert/info                │
│  ├─ GET /api/v1/doctors/psv/training/info                  │
│  ├─ GET /api/v1/doctors/psv/employment/info                │
│  ├─ GET /api/v1/doctors/psv/disciplines/check              │
│  ├─ GET /api/v1/doctors/psv/malpractice/history            │
│  └─ GET /api/v1/doctors/psv/verify                          │
│                                                              │
│  ACADEMIC ADMISSION (7)                                     │
│  ├─ POST /api/v1/academic/admin/students                    │
│  ├─ POST /api/v1/academic/admin/eligibility-rules           │
│  ├─ POST /api/v1/academic/admin/merit-rules                │
│  ├─ GET /api/v1/academic/verify/roll-number                │
│  ├─ GET /api/v1/academic/verify/certificate                │
│  ├─ GET /api/v1/academic/check-eligibility                 │
│  └─ GET /api/v1/academic/calculate-merit                   │
│                                                              │
│  INSURANCE CLAIMS (8)                                       │
│  ├─ POST /api/v1/insurance/admin/medical-enrichment         │
│  ├─ POST /api/v1/insurance/admin/reviewer-request           │
│  ├─ POST /api/v1/insurance/admin/payout-request             │
│  ├─ POST /api/v1/insurance/admin/notification               │
│  ├─ POST /api/v1/insurance/medical/enrich                   │
│  ├─ POST /api/v1/insurance/claims/review                    │
│  ├─ POST /api/v1/insurance/payout/process                   │
│  └─ POST /api/v1/insurance/notification/send                │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Technology Stack

```
┌────────────────────────────────────────────────┐
│           VERIFIABLE STUBS TECH STACK          │
├────────────────────────────────────────────────┤
│                                                 │
│  BACKEND FRAMEWORK                            │
│  └─ Flask 2.3.3                               │
│                                                 │
│  DATABASE ORM                                 │
│  └─ SQLAlchemy 2.0.21                         │
│                                                 │
│  DATABASE MIGRATIONS                          │
│  └─ Alembic 1.12.0                            │
│                                                 │
│  DATA VALIDATION                              │
│  └─ Pydantic 2.3.0                            │
│                                                 │
│  API DOCUMENTATION                            │
│  └─ Flasgger 0.9.7.1 (Swagger UI)            │
│                                                 │
│  DATABASE                                     │
│  └─ PostgreSQL 12+                            │
│  └─ psycopg2-binary 2.9.7 (driver)           │
│                                                 │
│  CORS SUPPORT                                 │
│  └─ Flask-CORS 4.0.0                         │
│                                                 │
│  ENVIRONMENT MANAGEMENT                       │
│  └─ python-dotenv 1.0.0                      │
│                                                 │
│  PRODUCTION SERVER                            │
│  └─ Gunicorn 21.2.0                          │
│                                                 │
│  CONTAINERIZATION                             │
│  └─ Docker                                     │
│  └─ Docker Compose                             │
│                                                 │
│  DEVELOPMENT                                  │
│  └─ Python 3.11+                              │
│  └─ Git                                        │
│                                                 │
└────────────────────────────────────────────────┘
```

---

## Deployment Architecture Options

### OPTION 1: AWS ELASTIC BEANSTALK (RECOMMENDED)
```
┌──────────────────────────────────────────┐
│  AWS Elastic Beanstalk                   │
│  ┌──────────────────────────────────┐   │
│  │  Auto-scaling EC2 Instances      │   │
│  │  (Running Flask + Gunicorn)      │   │
│  └────────────┬─────────────────────┘   │
│               │                          │
│               ▼                          │
│  ┌──────────────────────────────────┐   │
│  │  AWS RDS PostgreSQL              │   │
│  │  (Managed Database)              │   │
│  └──────────────────────────────────┘   │
│               │                          │
│               ▼                          │
│  ┌──────────────────────────────────┐   │
│  │  AWS CloudWatch                  │   │
│  │  (Monitoring & Logging)          │   │
│  └──────────────────────────────────┘   │
└──────────────────────────────────────────┘
```

### OPTION 2: AWS EC2 + RDS
```
┌──────────────────────────────────────────┐
│  AWS EC2 Instance                        │
│  ┌──────────────────────────────────┐   │
│  │  Flask App + Gunicorn            │   │
│  │  Linux OS (Ubuntu 22.04)         │   │
│  └──────────────────────────────────┘   │
│               │                          │
│               ▼                          │
│  ┌──────────────────────────────────┐   │
│  │  AWS RDS PostgreSQL              │   │
│  │  (Separate database server)      │   │
│  └──────────────────────────────────┘   │
│               │                          │
│               ▼                          │
│  ┌──────────────────────────────────┐   │
│  │  AWS S3 (Static files)           │   │
│  │  AWS CloudWatch (Logs)           │   │
│  └──────────────────────────────────┘   │
└──────────────────────────────────────────┘
```

### OPTION 3: DOCKER + ECS
```
┌──────────────────────────────────────────┐
│  AWS ECS (Container Orchestration)       │
│  ┌──────────────────────────────────┐   │
│  │  Task 1                          │   │
│  │  ┌──────────────────────────┐   │   │
│  │  │ Docker Container         │   │   │
│  │  │ (Flask App)              │   │   │
│  │  └──────────────────────────┘   │   │
│  └──────────────────────────────────┘   │
│  ┌──────────────────────────────────┐   │
│  │  Task 2, 3, ... (Auto-scaling)  │   │
│  └──────────────────────────────────┘   │
│               │                          │
│               ▼                          │
│  ┌──────────────────────────────────┐   │
│  │  AWS RDS PostgreSQL              │   │
│  │  (Shared database)               │   │
│  └──────────────────────────────────┘   │
│               │                          │
│               ▼                          │
│  ┌──────────────────────────────────┐   │
│  │  AWS ECR (Docker Registry)       │   │
│  │  AWS CloudWatch (Monitoring)     │   │
│  └──────────────────────────────────┘   │
└──────────────────────────────────────────┘
```

---

## Development Workflow

```
1. SETUP
   ├─ Create virtual environment
   ├─ Install dependencies
   ├─ Configure .env
   └─ Create database

2. DEVELOPMENT
   ├─ Write code (routes/models/schemas)
   ├─ Test with Swagger UI
   ├─ Test with API examples
   └─ Fix issues

3. DATABASE CHANGES
   ├─ Create Alembic migration
   ├─ Review migration
   ├─ Apply migration
   └─ Test changes

4. TESTING
   ├─ Test all endpoints
   ├─ Test error cases
   ├─ Verify database integrity
   └─ Check performance

5. DEPLOYMENT
   ├─ Update .env for production
   ├─ Run migrations on prod database
   ├─ Deploy application
   ├─ Configure CloudWatch
   └─ Monitor in production
```

---

## File Summary

| Category | Files | Count |
|----------|-------|-------|
| Python Code | .py | 15 |
| Configuration | .env, .json, .yml | 4 |
| Documentation | .md, .txt | 6 |
| Setup Scripts | .sh, .bat | 2 |
| Ignore Files | .gitignore | 1 |
| Docker | Dockerfile, docker-compose.yml | 2 |
| **TOTAL** | | **30+** |

---

## Key Numbers

| Metric | Count |
|--------|-------|
| Total API Endpoints | 26 |
| Database Tables | 18 |
| Database Models | 15 |
| Pydantic Schemas | 25+ |
| Route Handlers | 26 |
| Configuration Files | 1 (config.py) |
| Documentation Files | 6 |
| Setup Scripts | 2 |

---

## Code Statistics

| Category | Files | Lines |
|----------|-------|-------|
| Models | 3 | ~350 |
| Routes | 3 | ~700 |
| Schemas | 3 | ~200 |
| Config | 1 | ~70 |
| Entry Point | 1 | ~20 |
| **Total** | **11** | **~1,340** |

---

**This project is production-ready and fully documented!**
